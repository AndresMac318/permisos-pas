import * as internal from "stream";

export interface Empleado {
    cedula: internal;
    nombre: string;
    telefono: internal;
    email: string;
    direccion: string;
    rol: string;
    firma: string;
}